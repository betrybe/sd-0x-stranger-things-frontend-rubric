{
  "root": true,
  "extends": "trybe-frontend",
  "rules": {
    "react/jsx-max-depth": ["warn"], // remover no template , apenas pra passar no avaliador
    "max-lines-per-function": ["warn"], // remover no template , apenas pra passar no avaliador
    "no-unused-vars": ["warn"] // remover no template , apenas pra passar no avaliador
  }
}
